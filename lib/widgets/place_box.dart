import 'package:flutter/material.dart';

class PlaceBox extends StatelessWidget {
  final Widget widget;
  const PlaceBox({Key? key, required this.widget}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return widget;
  }
}
